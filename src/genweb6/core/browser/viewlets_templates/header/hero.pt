<tal:hero tal:define="pref_lang context/@@genweb.utils/pref_lang;
                      header_config context/@@genweb.utils/genwebHeaderConfig;
                      heroImage view/getHeroHeader;
                      style view/getClass">

    <div id="hero"
         tal:attributes="class style;
                         style string:--bg-img-url: url(${heroImage/src})"
         tal:define="is_new_style python:'pretty-image-hero' in style">

        <div class="site_title" tal:condition="not:is_new_style">
            <div class="h1">
                <a accesskey="1" class="text-decoration-none"
                   tal:attributes="href string:${view/root_url}/${view/pref_lang}">
                    <tal:block tal:replace="structure python:getattr(header_config, 'html_title_' + pref_lang, '')"></tal:block>
                </a>
            </div>
        </div>

        <img alt="" title="" tal:condition="python:heroImage and 'text-hero' not in style and 'full-pretty-image-hero' not in style"
             tal:attributes="src heroImage/src;
                             alt heroImage/alt;
                             title heroImage/title" />

        <div tal:condition="is_new_style" role="heading" aria-level="1">
            <div class="h1">
                <tal:block tal:replace="structure python:getattr(header_config, 'html_title_' + pref_lang, '')"></tal:block>
            </div>
            <div class="h2">
                <tal:block tal:replace="structure python:getattr(header_config, 'html_description_' + pref_lang, '')"></tal:block>
            </div>
        </div>

        <a href="#" class="down-hero" tal:condition="python:'full-pretty-image-hero' in style">
            <i class="bi bi-arrow-down-circle-fill"></i>
        </a>

    </div>

</tal:hero>
